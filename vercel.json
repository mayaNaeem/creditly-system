{
  "version": 2,
  "buildCommand": "npm run build && cd backend && npm install && npx prisma generate && npx tsc -p tsconfig.json",
  "outputDirectory": "dist/creditly-system/browser",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
